// Bump when bot-managed event messages or their roster image need to be
// republished. The sync state comparison then edits every existing message.
export const eventInfoMessageRenderVersion = "10"
