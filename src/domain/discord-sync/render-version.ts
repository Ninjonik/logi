// Bump when a bot-managed event-info message or its roster image needs to be
// republished. The sync state comparison then edits every existing message.
export const eventInfoMessageRenderVersion = "6"
