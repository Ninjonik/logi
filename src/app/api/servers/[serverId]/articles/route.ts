import { NextResponse } from "next/server";
import { getServerContext } from "@/lib/server-context";
import { listArticles, saveArticle } from "@/lib/articles";
export async function GET(_request:Request,{params}:{params:Promise<{serverId:string}>}) { const {serverId}=await params; const context=await getServerContext(serverId); if(!context)return NextResponse.json({error:"Forbidden."},{status:403}); return NextResponse.json({data:await listArticles(serverId)}); }
export async function POST(request:Request,{params}:{params:Promise<{serverId:string}>}) { const {serverId}=await params; const context=await getServerContext(serverId); if(!context?.canAdmin)return NextResponse.json({error:"Forbidden."},{status:403}); const body=await request.json(); try { const id=await saveArticle({guildId:serverId,authorId:context.user.discordId,title:String(body.title??""),description:String(body.description??""),tags:Array.isArray(body.tags)?body.tags.map(String):[],body:String(body.body??""),attachments:Array.isArray(body.attachments)?body.attachments.map(String):[]}); return NextResponse.json({id:String(id)},{status:201}); } catch(error) { return NextResponse.json({error:error instanceof Error?error.message:"Unable to save article."},{status:400}); } }
