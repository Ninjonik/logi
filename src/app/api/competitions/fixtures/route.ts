import { NextResponse } from "next/server";
import { isCurrentUserSuperadmin } from "@/lib/auth";
import { appCacheTags, revalidateCacheEntries } from "@/lib/cache-tags";
import { saveCompetitionFixture } from "@/lib/read-models/competitions";
export async function POST(request: Request) { if (!(await isCurrentUserSuperadmin())) return NextResponse.json({ error: "Forbidden." }, { status: 403 }); try { const body = await request.json(); const scoreA = body.scoreA === "" ? undefined : Number(body.scoreA); const scoreB = body.scoreB === "" ? undefined : Number(body.scoreB); if (!body.competitionId || !body.teamAId || !body.teamBId || body.teamAId === body.teamBId || (body.status !== "scheduled" && (!Number.isInteger(scoreA) || !Number.isInteger(scoreB)))) return NextResponse.json({ error: "Provide two teams and whole-number final scores." }, { status: 400 }); await saveCompetitionFixture({ competitionId: body.competitionId, divisionId: body.divisionId || undefined, phase: body.phase === "playoff" || body.phase === "relegation" ? body.phase : "league", teamAId: body.teamAId, teamBId: body.teamBId, scoreA, scoreB, status: body.status === "forfeit" ? "forfeit" : body.status === "final" ? "final" : "scheduled" }); revalidateCacheEntries([appCacheTags.competition("ecl-2026")]); return NextResponse.json({ ok: true }); } catch { return NextResponse.json({ error: "Unable to save fixture." }, { status: 500 }); } }
